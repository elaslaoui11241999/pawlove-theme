<nav class="navbar">
  <a href="{{ routes.root_url }}" class="nb-logo"><span class="paw">🐾</span>Paw<span>Love</span></a>
</nav>
<div style="background:var(--cream);min-height:80vh;display:flex;align-items:center;justify-content:center;text-align:center;padding:60px 24px">
  <div>
    <div style="font-size:80px;margin-bottom:20px">🐾</div>
    <div class="sec-tag" style="margin:0 auto 14px">Ups, pagina perdida</div>
    <h1 style="font-family:'Fraunces',serif;font-size:clamp(72px,12vw,130px);font-weight:900;line-height:1;margin-bottom:16px;color:var(--orange)">404</h1>
    <p style="color:var(--gray);font-size:15px;max-width:380px;margin:0 auto 36px">Esta pagina no existe, pero tenemos muchos productos para tu mascota.</p>
    <a href="{{ routes.root_url }}" class="btn-primary">Volver al inicio 🐾</a>
  </div>
</div>
